{
  "name": "@storige/docs",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Storige 파트너 문서 포털 — docs/PLATFORM_INTEGRATION_GUIDE.md 의 정적 발행 채널 (신규 정본 아님)",
  "scripts": {
    "build": "node build.mjs",
    "postbuild": "node ../../scripts/check-source-exposure.mjs site",
    "clean": "node -e \"require('node:fs').rmSync('site',{recursive:true,force:true})\""
  },
  "devDependencies": {
    "marked": "^18.0.7"
  }
}
