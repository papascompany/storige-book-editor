{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "cd ../.. && pnpm install --no-frozen-lockfile --filter @storige/admin... --ignore-scripts",
  "buildCommand": "cd ../.. && pnpm --filter @storige/types build && pnpm --filter @storige/admin build",
  "outputDirectory": "dist",
  "ignoreCommand": "P=\"${VERCEL_GIT_PREVIOUS_SHA}\"; if [ -z \"$P\" ] || ! git cat-file -e \"$P^{commit}\" 2>/dev/null; then exit 1; fi; git diff --quiet \"$P\" HEAD ./ ../../packages/types ../../packages/ui ../../packages/indesign-import",
  "build": {
    "env": {
      "VITE_API_BASE_URL": "https://api.papascompany.co.kr/api"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
