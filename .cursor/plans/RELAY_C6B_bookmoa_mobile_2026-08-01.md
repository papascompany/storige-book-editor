# [발송용] bookmoa-mobile팀 — Storige 편집기 롤아웃 안내: 삭제 확인 모달(C6-b)·다중선택 복제(C5)·분배 단축키 (2026-08-01)

> 발송 채널: 오너(yohan)가 bookmoa-mobile 연동 세션/파트너 담당자에게 직접 전달. 시크릿 없음.

---

안녕하세요, Storige(papascompany) 입니다.

지난 안내(2026-07-24, C5·C6·C9 롤아웃)에서 **"C6-b는 미포함, 도입 시 별도 통지"**로 유보했던 항목이 도입되어 안내드립니다. 아울러 경미한 변경 2건도 함께 정리했습니다.

## 배포 정보

- 배포 대상: editor.papascompany.co.kr (iframe `/embed` 포함) — bookmoa-mobile 임베드 편집기에 **자동 반영**
- 배포 기준: 2026-07-24 반영 완료 + **2026-08-01 컨텍스트 메뉴 해제 동작 보완** 반영, 현재 라이브
- bookmoa-mobile 측 코드 변경: **불필요** (postMessage 계약·canvasData 직렬화 불변)

## 변경 사항

### 1. C6-b — 컨텍스트 메뉴 '삭제' 확인 모달 경유 ★모바일 체감 있음

- **이전**: 컨텍스트 메뉴(모바일 롱프레스 / 데스크탑 우클릭)의 '삭제'가 확인 없이 **즉시 삭제**.
- **이후**: '삭제' 선택 시 **삭제 확인 모달 1스텝 경유** — DEL 키 삭제와 동일한 안전 흐름으로 통일.
- **효과**: 모바일 오탭·데스크탑 오클릭 삭제 방지(안전 강화 방향). 지난 안내의 모바일 롱프레스 메뉴(C6) 위에 얹히는 변경이라, **모바일 사용자 체감이 있으니 QA·CS 공유를 권장**드립니다.
- **범위**: storige 편집기(iframe 임베드) 경유는 모달 적용. (참고: canvas-core를 직접 사용하는 자체 편집기 케이스는 어댑터 미주입으로 기존 즉시 삭제 유지 — bookmoa-mobile은 iframe 임베드이므로 모달 적용 대상입니다.)
- **추가 보완 (2026-08-01 배포)**: 모바일 컨텍스트 메뉴의 해제 동작 2건을 함께 손봤습니다 —
  ① 메뉴 밖 빈 곳을 탭하면 메뉴가 닫힙니다(종전에는 닫히지 않았습니다),
  ② 삭제 확인 모달이 열릴 때 메뉴가 자동으로 닫혀 모달 버튼을 가리지 않습니다.
- **Storige 측 검증 완료 (2026-08-01)**: iOS Safari(iPhone 17 Pro · iOS 26.5 시뮬레이터)에서 실제 터치
  입력으로 **롱프레스 → 컨텍스트 메뉴 → '삭제' → 확인 모달 → 취소** 전 구간을 확인했습니다.
  Android Chrome 은 동일 엔진 계열 자동 검증까지 완료했습니다.

### 2. C5 — Alt+드래그 복제 다중선택 확장 (경미)

- 기존 C5(단일 객체 Alt+드래그 복제)의 확장 — **다중선택 상태에서도 Alt+드래그로 선택 전체 복제**. 원본은 드래그되어 나가고 사본이 시작 위치에 잔류(Canva 동일 시맨틱).
- 데스크탑 한정(Alt 키). **모바일 무관**.

### 3. D-E2-1 — 분배 단축키 (릴리스 노트 수준)

- 3개 이상 선택 시 균등 분포 단축키 신설: `Alt+Shift+H`(가로 균등), `Alt+Shift+V`(세로 균등). 기존 정렬 버튼과 동일 동작. 데스크탑 한정.

## 요청 사항 (권장, 부담 없는 수준)

1. 위 흐름은 Storige 측에서 확인을 마쳤으니, **실기기(iOS Safari / Android Chrome) 최종 확인 1회**만 부탁드립니다.
2. C6-b 동작 변경(삭제가 즉시가 아닌 확인 경유)을 QA·CS에 공유해 주시면 사용자 문의 대비에 좋습니다.
3. 이상 발견 시: 증상 + 세션 ID + 기기/뷰포트를 회신해 주세요.

## 롤백 (참고)

- C5 다중: 기능 플래그로 Storige 측 즉시 off 가능(단일/다중 동시 off).
- C6-b: 코드 레벨(전용 플래그 없음) — 되돌림 시 재배포. 단 확인 모달은 안전 강화라 롤백 실익 낮음.
- D-E2-1: 단축키만(기능 무해), 롤백 불요.

궁금한 점이나 이상 동작이 있으면 언제든 회신 부탁드립니다.

감사합니다.
Storige 드림 (papascompany / yohan)
