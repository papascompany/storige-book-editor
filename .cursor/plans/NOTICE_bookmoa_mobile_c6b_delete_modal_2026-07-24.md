# [통지] Storige 편집기 — 컨텍스트 메뉴 삭제 확인 모달(C6-b) + 다중선택 복제(C5) 롤아웃 (2026-07-24)

> 직전 통지(`NOTICE_bookmoa_mobile_c5c6c9_rollout_2026-07-24`, line 34)에서 **"C6-b는 미포함, 도입 시 별도 통지"**로 유보했던 항목의 **도입 통지**입니다. 오너가 임베드 파트너(bookmoa-mobile 등)에게 relay 대상.

## 대상
임베드 편집기 사용처: **bookmoa-mobile · ShareSnap** 등 (canvas-core 공통 → 자동 반영). 배포=editor.papascompany.co.kr, master `bdef580`.

## 변경 사항

### 1. C6-b — 컨텍스트 메뉴 '삭제' 확인 모달 경유 ★체감 있음
- **이전**: 컨텍스트 메뉴(모바일 롱프레스 / 데스크탑 우클릭)의 '삭제'가 확인 없이 **즉시 삭제**.
- **이후**: '삭제' 선택 시 **삭제 확인 모달 1스텝 경유** — DEL 키 삭제와 동일한 안전 흐름으로 통일.
- **효과**: 모바일 오탭·데스크탑 오클릭 삭제 방지(안전 강화 방향).
- **범위**: storige 편집기(iframe 임베드) 경유는 모달 적용. 외부 임베더가 canvas-core를 직접 사용(자체 편집기)하는 경우는 어댑터 미주입으로 기존 즉시 삭제 유지(무영향).

### 2. C5 — Alt+드래그 복제 다중선택 확장 (경미)
- 기존 C5(단일 객체 alt+드래그 복제)의 확장 — **다중선택 상태에서도 alt+드래그로 선택 전체 복제**. 원본은 드래그되어 나가고 사본이 시작 위치에 잔류(Canva 동일 시맨틱).
- 데스크탑 한정(alt 키). 모바일 무관.

### 3. D-E2-1 — 분배 단축키 (릴리스 노트 수준)
- 3개 이상 선택 시 균등 분포 단축키 신설: `Alt+Shift+H`(가로 균등), `Alt+Shift+V`(세로 균등). 기존 정렬 버튼과 동일 동작.

## 롤백
- C5 다중: `VITE_ENABLE_ALT_DRAG_CLONE=false` (단일/다중 동시 off)
- C6-b: 코드 레벨(전용 플래그 없음) — 되돌림 시 재배포. 단 확인 모달은 안전 강화라 롤백 실익 낮음.
- D-E2-1: 단축키만(기능 무해), 롤백 불요.

## 참고
- 대응 커밋: `93d2910`(D-E2-1) · `e2b8782`(C6-b) · `bbb2e9a`(C5) · `bdef580`(강화).
- 실기 QA 잔여: C6(롱프레스) 실 iOS Safari/Android Chrome 시나리오(별도 §2-1) — C6-b는 그 위에 얹힘.
