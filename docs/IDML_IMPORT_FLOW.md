# InDesign(IDML) 가져오기 — 업로드·변환·등록·테스트·검증 전체 플로우

> **목적**: 인디자인 파일을 Storige 템플릿/템플릿셋으로 가져오는 **운영 전체 흐름**과 **검증 체크리스트**.
> 기술 아키텍처는 [`DESIGN_IMPORT_CONVERTER.md`](DESIGN_IMPORT_CONVERTER.md) 참조(이 문서는 "어떻게 실제로 돌리고 검증하나").
> **갱신**: 2026-06-12 · 변환 모드 3종(conversionMode) + placed 이미지 동반 업로드(A5) + 충실도 사이클(A1~A6) 배포 직후.

---

## 0. 두 가지 산출물 (무엇을 만드나)

| 산출물 | 설명 | 등록 경로 |
|---|---|---|
| **① 펼침면 단품 템플릿** | 표지 펼침면 1장(날개+앞표지+책등+뒤표지+날개) 디자인을 그대로 템플릿으로. 같은 사이즈 여러 표지 디자인을 교체용으로 모음 | `POST /templates`(type=spread) |
| **② 책등 가변 템플릿셋** | 표지(펼침면) + 내지로 구성된 **책** 셋. 내지 페이지 수에 따라 **책등 폭이 런타임 자동 변동** | 방법 A: 표지 Template 등록 → 셋 폼 인계 |

> "펼침면을 이미지로 누르는" 케이스 = **플랫형 변환 모드 2종**(아래 §1[3]): 텍스트만 편집 레이어로 살리고 나머지 디자인은 300dpi PNG 로 평면화. **펼침면 플랫형**(1장, 책등 고정) / **책등가변 3분할 플랫형**(3장, 책등 가변 추종) 중 상품 특성으로 선택.

---

## 1. 전체 플로우 (7단계)

```
[1] IDML 준비   →  [2] admin 업로드  →  [3] 변환(벡터/하이브리드)  →  [4] 미리보기·경고 확인
        →  [5] 등록(단품 Template / 책등가변 셋)  →  [6] 수동 보완  →  [7] 편집기 테스트·검증
```

### [1] IDML 준비 (InDesign)
- InDesign에서 **IDML로 내보내기**(`파일 > 다른 이름으로 저장 > InDesign Markup (IDML)`). `.indd`는 직접 미지원 → `scripts/indd-to-idml.jsx`(ExtendScript)로 IDML 변환 후 입력.
- ⚠️ **폰트 미임베드**: IDML은 폰트 참조만 담음. 변환·인쇄 시 폰트가 라이브러리에 있어야 정확. (최종 인쇄는 텍스트를 **아웃라인 벡터화**하므로 폰트가 라이브러리에 로드돼 있으면 정확히 출력 — §5 참조)
- 표지 펼침면은 **날개/앞표지/책등/뒤표지/날개 5영역**이 한 스프레드에 있어야 함.

### [2] admin 업로드
- admin → **템플릿 > 템플릿관리 → "IDML 가져오기"** 버튼 → 라우트 `/templates/import`.
- 파일 드롭/선택. **포맷 자동감지**: `.idml` → 펼침면/단품, `.psd` → 단일 페이지(명함/내지 단품), `.ai` → 미지원(SVG 내보내기 우회 권장).
- **배치(placed) 이미지 동반 업로드** (2026-06-12, A5) — IDML 에는 이미지 픽셀이 없어(링크 메타만) 종전엔 회색 플레이스홀더였으나, 링크 이미지를 함께 주면 **자동 복원**(프레임 크롭/플립까지 베이크):
  1. **`.idml` 단독** — 기존과 동일(플레이스홀더+경고).
  2. **`.idml` + 이미지 다중 선택** — 같은 드롭에 JPG/PNG 등 이미지를 함께 넣으면 **파일명으로 자동 매칭**(NFC 정규화, 대소문자 무시).
  3. **패키지 zip** — `*.idml`+이미지를 zip 1개로 업로드(InDesign 패키지 Links 폴더 구조 무관, 파일명 기준). TIFF/EPS/PDF 등 브라우저 미지원 형식은 건너뛰고 안내.
  4. **이미지 추가 업로드** — 변환 후 매칭 실패 목록을 보고 이미지를 추가로 올리면 **직전 변환에 병합 재변환**.
  - 변환 결과에 **매칭 ✓/✗ 요약 행**(링크별 사유, ×N프레임) 표시 — ✗ 는 같은 파일명(JPG/PNG 변환)으로 재업로드.

### [3] 변환 — 모드 3종 선택 (2026-06-12 확장)
브라우저에서 즉시 변환(jszip+fast-xml-parser). **Segmented 토글로 모드 선택** — 문구는 admin UI 와 동일:

| 모드(admin UI 문구) | 결과 | 언제 |
|---|---|---|
| **벡터 (전체 편집형)** | 도형·텍스트를 모두 편집 가능한 벡터로 추출(정밀, svg2pdf 무손실 경로). CMYK `cmykFill` 보존 | 대부분 권장. 단순한 디자인, 객체 미세조정 필요 시 |
| **펼침면 플랫형 (책등 고정)** | 텍스트만 편집 레이어 + 나머지 디자인을 **전폭 300dpi PNG 1장**으로 고정 | 복잡한 효과/그라디언트가 많아 벡터 재현이 어려울 때("펼침면을 이미지로") + **책등 두께가 고정인 상품**(페이지 수 변동 없음). 편집기에서 책등 가변이 차단됨 |
| **책등가변 3분할 플랫형** | 텍스트만 편집 레이어 + 디자인을 **뒷표지/책등/앞표지 3장의 300dpi PNG** 로 분할 고정. 책등 PNG 는 책등 중심 기준 **3배폭**이라 페이지 수에 따라 책등 두께가 변해도 디자인이 따라감 | 복잡한 디자인을 **책등 가변 책 셋**으로 쓸 때(권장) |

- 선택 결과는 `spreadConfig.conversionMode`(full/flat-spread/flat-spine)로 저장된다 — 레거시 템플릿(미존재)은 full 로 동작. 기술 상세: [`DESIGN_IMPORT_CONVERTER.md`](DESIGN_IMPORT_CONVERTER.md) §3.5/§13.
- ⚠️ **펼침면 플랫형으로 등록한 표지를 책등 가변 셋에 넣으면 책등이 변하지 않는다**(의도된 가드 — 통짜 PNG 보호). 책등 가변이 필요하면 **책등가변 3분할 플랫형**으로 재변환.

> 플랫형/PSD 배경 PNG는 **스토리지에 업로드**되어 `canvasData`엔 URL만 저장(인라인 base64 제거 → DB 비대화 방지, 2026-06-09). 3분할 플랫형은 spine/back/front 3장이 각각 업로드된다.

### [4] 미리보기·경고 확인
- 변환 결과 **SVG 미리보기** + **검수 경고** 표시:
  - **별색(Spot)** 감지 경고 / **풀블리드 커버리지**(채움이 재단선 밖까지 닿는지) / 컴파운드패스 even-odd 등.
  - 책등 세로글씨(90°), 줄바꿈, 폴리곤 베지어 경로가 원본대로인지 눈으로 확인.

### [5] 등록 (등록 대상 라디오)
- **표지 단품**: 그대로 `POST /templates`(type=spread). 끝.
- **책등 가변 셋**(방법 A): 표지 Template 저장 후 `navigate('/template-sets/new', {state:{seedTemplateId}})` 로 **템플릿셋 폼 인계**. 폼이 표지를 셋에 자동 추가(required) + 책모드 + 판형(=표지 한 면 coverWidth/Height) 자동 설정.

### [6] 수동 보완 (자동 불가 항목)
| 항목 | 이유 |
|---|---|
| **내지 템플릿 추가** | IDML 표지에 내지가 없음. book 검증=SPREAD 정확히 1개 + PAGE ≥1개 필수 |
| **pageCountRange** | 내지 수량 범위는 상품 정책(IDML에 없음) |
| **PDF 출력 모드 / 색 처리 모드** | 단면/양면, RGB/CMYK는 상품별 설정(템플릿셋 폼 '표지·PDF 출력' 패널) |
| **에셋 구성** | 노출 라이브러리 카테고리(비우면 전역) |
| **상품 연결** | `ProductTemplateSet`(sortcode) — 이 흐름 밖 별도 단계 |

### [7] 편집기 테스트·검증
- 등록된 셋으로 **편집 세션** 진입(고객 주문 또는 테스트 세션). 책모드면 스프레드 편집.
- 책등 가변·색·폰트·인쇄 출력 검증(§4, §5).

---

## 2. 시나리오별 상세

### 2.1 시나리오 A — 펼침면 단품 템플릿 (플랫형 "이미지로 누르기")
1. IDML 업로드 → **펼침면 플랫형 (책등 고정)** 선택 → 텍스트만 편집 레이어 + 배경 PNG.
2. 미리보기에서 텍스트 위치/폰트 + 배경 이미지 확인.
3. 등록 대상 = **표지 단품** → `POST /templates`.
4. (옵션) 같은 사이즈의 다른 표지 디자인들을 추가로 변환·등록 → 편집기 '템플릿' 메뉴에서 **교체 가능**(③ 커버 교체 워크플로우).
5. 검증: 편집기에서 텍스트 미세조정(폰트/크기/효과) → 저장 → 인쇄 PDF 출력 확인.

### 2.2 시나리오 B — 책등 가변 템플릿셋
1. IDML 업로드 → **벡터** 또는 **책등가변 3분할 플랫형**(복잡한 디자인) → 등록 대상 = **책등 가변 셋**. (⚠️ '펼침면 플랫형'은 책등 고정이라 이 시나리오에 부적합 — §1[3])
2. 표지 Template 저장 → 셋 폼 자동 인계(표지 seed + 책모드 + 판형).
3. **수동**: 내지 템플릿 추가 + `pageCountRange`(예: `[10,20,30,40]`) + PDF/색 모드 설정.
4. 셋 저장 → 상품 연결(ProductTemplateSet).
5. **책등 가변 검증**(핵심): 편집 세션에서 **내지 페이지 수 변경** → 책등 폭이 `(pageCount/2)×thickness + margin` 으로 자동 재계산 + 앞/뒤표지가 책등 변화만큼 평행이동(3분할 플랫형은 back/front PNG 평행이동 + 책등 3배폭 PNG 노출). 편집완료→재로드 후에도 유지(`meta` 직렬화 보존).

---

## 3. 책등 가변 원리 (왜 자동인가)
- 책등 폭은 **템플릿 고정값이 아니라 런타임 파생값**: `spine.service.ts` `spineWidth=(pageCount/2)×thickness+margin`.
- 권위 검증 `validateSpreadAgainstAuthority`(types)는 **coverWidthMm/coverHeightMm/wingEnabled/wingWidthMm 4필드만** 비교(spineWidthMm/totalWidthMm는 의도적 제외).
- → IDML 원본의 고정 책등은 버리고 **표지 한 면(cover)+날개(wing)만 권위로 추출**하면 자연스럽게 가변화. 책등 텍스트는 중앙 재배치, 앞/뒤표지는 평행이동(리플로우 손실 없음).

---

## 4. 검증 체크리스트

### 4.1 변환 충실도 (admin 미리보기 + 편집기)
- [ ] z-순서(쌓임) 원본대로(원형이 책등 패널 위로 안 올라오는지 등)
- [ ] 책등 제목/저자 **세로글씨(90°)** 정상
- [ ] 뒷면 출판사 등 **여러 줄 텍스트** 줄바꿈 보존(영역 이탈 없음)
- [ ] 폴리곤/곡선 **베지어 경로** 복원
- [ ] mm 단위 치수 정확(총폭 = 날개+앞+책등+뒤+날개)

### 4.2 인쇄 규약 (이번 사이클 신규 — §5)
- [ ] **색 처리**: 셋 `colorMode` 설정대로(RGB 유지 / CMYK 변환). ※ 워커 색변환은 적용 후 검증
- [ ] **텍스트**: 인쇄 PDF에서 텍스트가 **아웃라인 벡터**(폰트 의존 없음), 혼합폰트 각 run 제 폰트
- [ ] **overprint/별색**: 고객 PDF의 `/OP`·Separation이 출력에 보존
- [ ] **프레임**: 사진을 프레임에 채우면 **투명창에만** 보이고 밖은 가려짐

### 4.3 책등 가변 / 워크플로우
- [ ] 내지 수 변경 → 책등 폭 자동 재계산 + 앞/뒤표지 평행이동
- [ ] 편집완료→재로드 후 책등 가변 재배치 유지(`meta` 보존)
- [ ] 같은 사이즈 다른 표지 펼침면으로 **교체**(편집기 '템플릿' 메뉴)

---

## 5. 이번 사이클 배포 기능 (2026-06-09, 가져온 디자인 출력에 직접 영향)

| 기능 | 효과 | 상태 |
|---|---|---|
| **상품별 색 처리 모드** `colorMode(rgb/cmyk)` | 템플릿셋별로 출력 색공간 지정 | ✅ admin/데이터모델 배포. 워커 실제 변환은 적용 예정 |
| **텍스트 아웃라인(혼합폰트 충실도)** | 텍스트를 벡터 아웃라인으로 출력(폰트 미임베드 무관), 혼합폰트 각 폰트 정확 | ✅ 배포(merge 20ac84c) |
| **overprint/별색 보존** | 고객 PDF의 overprint·별색이 재distill 시 소실되지 않음 | ✅ 배포(워커) |
| **사진 프레임 마스킹** | 프레임 투명창에만 사진(inverted clipPath) | ✅ 배포 |
| **변환 PNG 스토리지 업로드** | 하이브리드/PSD 배경을 URL로 저장(DB 비대화 방지) | ✅ 배포 |
| **PDF 출력 모드** `pdfOutputMode` (단면/양면-원파일/양면-파일분리) | 낱장 상품 PDF 생성 방식 | ✅ admin. 워커 duplex-split 배포(셋이 선택 시 작동) |

> ⚠️ 위 인쇄출력 기능은 **실주문/시각 검증 중**(deploy-and-iterate). 문제 발견 시 §6.

---

## 5b. 재편집 정합성 사이클 (2026-06-11, 등록→재편집→저장 무결성)

가져온 템플릿을 **다시 '편집'으로 열었을 때** 객체가 투명/붕괴되거나, 미리보기·하이브리드 래스터가
좌우로 어긋나거나, 배경 아트워크가 이동되던 5종 결함을 수정(서브에이전트 교차검증, commit `527b85b`).

| # | 증상 | 근본원인 | 수정 |
|---|---|---|---|
| ① | 재편집 시 객체가 투명(선택 프레임만)·일부만 보임 | `loadJSON` 이 `canvasData.width/height`(판형 메타)를 fabric 캔버스 px 치수로 덮어씀 → `skipOffscreen` 컬링이 화면 밖 객체 렌더 스킵. IDML 템플릿은 workspace 객체가 없어 `afterLoad` 의 치수 복구도 누락 | `ServicePlugin.loadJSON` width/height strip + `WorkspacePlugin.afterLoad` workspace 복원 + `SpreadPlugin.afterLoad` 가이드 재렌더 |
| ② | 저장된 좌표가 영구 변위(특정 객체만) | ①로 깨진 화면에서 **사용자가 객체를 드래그**한 산물이 DB 박제(코드 버그 아님) | ① 수정으로 화면 정상화 → 재발 방지. 이미 변위된 템플릿은 **재가져오기** 필요 |
| ③ | admin 임포트 미리보기에서 앞표지 패널이 통째로 안 보임 | `preview/svg.mjs` 가 중앙원점 객체를 좌상단 viewBox 에 보정 없이 그림(a92f146 누락) | 비-path 객체에 `+halfW/+halfH` 보정 |
| ④ | 하이브리드 PNG 표지 좌우가 뒤바뀜 | `raster/rasterize.mjs` 동일 누락 | 동일 보정 |
| ⑤ | 배경 아트워크가 이동·삭제됨 | 변환기가 `selectable:true` 로 emit | `ARTWORK_LOCK`(고정·패널숨김·관리자잠금) emit + `useTemplateSetSave` 직렬화 전체 보존 |
| ⑥ | 세로쓰기 제목이 거대한 가로 텍스트로 렌더·캔버스 밖 잘림 | InDesign **세로짜기**(`StoryPreference@StoryOrientation="Vertical"`)를 리더가 미파싱 — 회전(90°) 책등과 다른 메커니즘 | 리더 `story.vertical` 파싱 + 변환기 글자 단위 세로 배치(한 글자=한 줄) 근사 + 검수 경고. 상세: `.cursor/plans/DEBUG_IDML_VERTICAL_TEXT_2026-06-11.md` |

> **배포 측 함정 동반 수정**: `apps/admin/vercel.json` 의 `ignoreCommand` 에 `packages/indesign-import` 가
> 빠져 있어 **변환기만 고치면 admin 이 재배포되지 않던** 문제 해소(등록 하이브리드 `styles` 누락의 배포측 원인).
> 변환기 수정 후 admin 배포가 안 보이면 이 항목 확인.

> ⚠️ **이미 등록된 깨진 템플릿**(2026-06-11 이전 변환·또는 ②로 변위 저장)은 코드 수정으로 자동 복원되지 않음 →
> **재가져오기**(또는 DB 좌표 패치) 필요. 신규 가져오기는 정상.

---

## 6. 문제 해결 (자주 생기는 이슈)

| 증상 | 점검 |
|---|---|
| 변환 텍스트 폰트가 다름/깨짐 | 해당 폰트가 라이브러리에 시딩됐는지. 미임베드 폰트는 폴백 → 아웃라인 시 base 폰트(또는 래스터) |
| **표지가 회색 박스투성이로 깨져 보임** | 배치(placed) 이미지 — IDML 에는 픽셀이 없음(경고 표시됨). **2026-06-12 이후: 링크 이미지를 동반 업로드(다중/zip)하면 자동 복원**(§1[2]). 동반 업로드 없이는 편집기 [이미지] 메뉴로 원본 교체. 텍스트/도형/좌표는 정상 |
| **경고 "동반 업로드 매칭 실패: 파일명"** | 같은 파일명의 이미지가 업로드 목록에 없음 → 파일명 그대로(확장자 포함) 추가 업로드하면 병합 재변환·자동 복원. "복원 미지원" 사유면 TIFF/EPS/PDF 등 브라우저 미지원 형식 — **JPG/PNG 로 변환해 재업로드** |
| **경고 "자간(Tracking)/단락 정렬(Justification) 혼합 … 지배값 근사"** | 한 텍스트 프레임 안에 서로 다른 자간/정렬이 섞임 — fabric 은 객체 단위만 지원해 **가장 많은 글자의 값으로 근사**(정상 동작). 편집기에서 분리 배치로 보완 가능 |
| **경고 "가로비율(HorizontalScale) N% … 미적용"** | 장평은 fabric 미지원 — 원본보다 글자폭이 넓게 보일 수 있음(검수 확인) |
| **경고 "미해석 색상 / 미해석 텍스트 색상 … 검정 대체"** | 색상 사전에 없는 색 id(텍스트 그라디언트 포함) — 도형 그라디언트는 2026-06-12부터 변환되나 **텍스트 fill 그라디언트는 검정 대체**(보류). 편집기에서 색 지정 |
| **경고 "그라디언트 … 확인 권장" / "코너/스트로크 … 근사"** | 실측 표본이 없는 케이스(회전/플립 그라디언트, 비대칭 코너, 점선 등)의 감지 경고 — 미리보기·편집기에서 해당 객체만 눈으로 확인 |
| **세로쓰기 제목이 가로로 거대하게/잘려 보임** | §5b ⑥ — 2026-06-11 수정 이후 신규 가져오기는 글자 단위 세로 배치로 근사. 그 이전 등록본은 재가져오기 |
| **재편집 시 객체가 투명/사라짐** | §5b ① — 2026-06-11(`527b85b`) 이후 신규 가져오기는 정상. 그 이전 등록본은 **재가져오기**. 배포 반영은 새 탭/캐시버스트로 확인 |
| **미리보기/플랫 PNG 좌우 어긋남** | §5b ③④ — `527b85b` 이후 정상. admin 배포 반영 확인(`ignoreCommand` 수정 포함) |
| **배경 아트워크가 움직임/지워짐** | §5b ⑤ — 신규 가져오기는 `template-element` 로 고정. 교체는 재가져오기 |
| 객체가 영역 밖으로 이탈 | 미리보기에서 확인 → 편집기 템플릿편집 모드로 미세조정(배경 아트워크는 고정, 텍스트만 편집) |
| 책등이 안 변함 | ① **'펼침면 플랫형 (책등 고정)' 템플릿이면 정상 가드**(§1[3] — 가변 필요 시 '책등가변 3분할 플랫형'으로 재변환) ② 책모드 셋인지, SPREAD 1개+PAGE≥1개인지, `meta`(regionRef) 보존됐는지 |
| 재편집 진입만으로 책등이 얇아짐/객체 재배치 | 2026-06-12(`e4eb328`) 수정 — spine 입력 복원 우선순위 + 자동저장 게이트. 이전 오염 세션은 주문 옵션 재지정 후 저장 |
| 편집 중 객체가 옆 패널 폭만큼 순간이동 | 2026-06-12(`a01f3f3`) PointerShiftGuard 로 차단 — 재발 시 콘솔 `[PointerShiftGuard]` 로그와 함께 보고 |
| 책 셋 저장 안 됨 | book 검증=SPREAD 정확히 1개 필수 → 내지(PAGE) 추가했는지 |
| 표지 색이 화면과 다름 | RGB 파이프라인 색차. 셋 `colorMode=cmyk` + 워커 색변환(적용 후) |
| 프레임에 사진이 삐져나옴/반대로 가려짐 | 프레임 마스킹 방향(inverted) 검증 항목 — 반대면 보고 |
| 변환 PNG가 안 보임 | 스토리지 업로드 URL(`/storage/...`) 접근성(CORS) 확인 |

---

## 7. 관련 문서 / 코드
- [`COORDINATE_SYSTEM.md`](COORDINATE_SYSTEM.md) — 좌표계 규약(동결): content↔scene(중앙원점) 변환 SSOT, width/height 함정
- [`DESIGN_IMPORT_CONVERTER.md`](DESIGN_IMPORT_CONVERTER.md) — 변환기 기술 아키텍처 + 로드맵 §11
- `apps/admin/src/pages/Templates/TemplateImport.tsx` — 가져오기 UI
- `apps/admin/src/pages/TemplateSets/TemplateSetForm.tsx` — 셋 폼(방법 A 인계 + 색/PDF/에셋 설정)
- `packages/indesign-import/` — 변환 코어(IDML/PSD 리더, spread/단일페이지 변환기)
- `.cursor/plans/RESUME_PROMPT_2026-06-09.md` — 세션 핸드오프(브랜치/배포 상태)

---

> 작성: 2026-06-09 · 갱신: 2026-06-12(§0/§1[2][3]/§2/§6 — 변환 모드 3종 `7585e38` + 동반 업로드 `e1fd2f2` + 충실도 경고 `8a23f93`/`3639c8b`/`50dc6d7` + 재편집/포인터 P1 `e4eb328`/`a01f3f3`). 실 플로우 테스트로 이슈 발견 시 §6 갱신.
